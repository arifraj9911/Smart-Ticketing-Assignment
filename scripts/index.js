const seatListOne = document.getElementById("seat-list-one");
const seatListTwo = document.getElementById("seat-list-two");
const seatListThree = document.getElementById("seat-list-three");
const seatListFour = document.getElementById("seat-list-four");
// console.log(seatListOne);

seatList(seatListOne);
seatList(seatListTwo);
seatList(seatListThree);
seatList(seatListFour);

// seatListOne.addEventListener("click", function (event) {
//   const specificSeat = event.target.innerText;
//   const seat = getSeatId(specificSeat);
//   if (!seatArray.includes(seat) && seatArray.length < 4) {
//     seatArray.push(seat);
//     setBackgroundColorById(seat);
//   }
// });

// seatListTwo.addEventListener("click", function (event) {
//   const specificSeat = event.target.innerText;
//   const seat = getSeatId(specificSeat);
//   if (!seatArray.includes(seat) && seatArray.length < 4) {
//     seatArray.push(seat);
//     setBackgroundColorById(seat);
//   }
// });
// seatListThree.addEventListener("click", function (event) {
//   const specificSeat = event.target.innerText;
//   const seat = getSeatId(specificSeat);
//   if (!seatArray.includes(seat) && seatArray.length < 4) {
//     seatArray.push(seat);
//     setBackgroundColorById(seat);
//   }
// });
// seatListFour.addEventListener("click", function (event) {
//   const specificSeat = event.target.innerText;
//   const seat = getSeatId(specificSeat);
//   if (!seatArray.includes(seat) && seatArray.length < 4) {
//     seatArray.push(seat);
//     setBackgroundColorById(seat);
//   }
// });
